# 루트 도메인은 대표 주소(www)로 통일한다.
blackapple.store {
    redir https://www.blackapple.store{uri} permanent
}

www.blackapple.store {
    encode zstd gzip

    @backend path /api/* /swagger-ui* /v3/api-docs*
    reverse_proxy @backend backend:8080
    reverse_proxy frontend:3000
}
